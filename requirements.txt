scapy>=2.5.0
python-nmap>=0.7.1
# netifaces>=0.11.0  # Опционально: требует Microsoft C++ Build Tools на Windows
#                    # Код имеет fallback через ipconfig, поэтому не обязателен
aiofiles>=23.2.0
requests>=2.31.0

